import { FilterconditionPipe } from './filtercondition.pipe';

describe('FilterconditionPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterconditionPipe();
    expect(pipe).toBeTruthy();
  });
});
